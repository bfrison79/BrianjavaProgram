package atHomePractice.Day1;

import java.util.Locale;

public class Practice {
    public static void main(String[] args) {
        String name = "Brian Frison";

        //boolean result = name.equalsIgnoreCase("Brian");
        //int result = name.length();
        //char result = name.charAt(1);
        int  result = name.indexOf("o");
        //boolean result =name.isEmpty();
        //String result = name.toLowerCase(Locale.ROOT);
        //String result = name.toUpperCase(Locale.ROOT);
        //String result = name.trim();
        //String result = name.replace('i', 'y');

        System.out.println(result);
    }
}
